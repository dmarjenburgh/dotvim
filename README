Installation:

    git clone git@github.com:dmarjenburgh/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Install the Inconsolata and Liberation Mono powerline fonts:

    https://github.com/Lokaltog/powerline-fonts    

